# API Test Suite

## Purpose
This folder holds backend API tests for request/response behavior.

## Intent
- Validate externally visible API behavior and status semantics.
- Mock network and SMTP boundaries so tests stay deterministic.
- Catch regressions in health, readiness, lookup, and contact flows.
